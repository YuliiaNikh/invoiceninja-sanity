{
  "name": "invoiceninja-studio",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "sanity dev",
    "start": "sanity start",
    "build": "sanity build",
    "deploy": "sanity deploy",
    "seed:pages": "node scripts/seed-pages.cjs",
    "seed:pages:cli": "sanity exec scripts/seed-pages-exec.mjs --with-user-token",
    "dedupe:pages": "sanity exec scripts/dedupe-pages-exec.mjs --with-user-token",
    "dedupe:pages:apply": "sanity exec scripts/dedupe-pages-exec.mjs --with-user-token -- --apply"
  },
  "dependencies": {
    "sanity": "^5.1.0",
    "@sanity/vision": "^5.1.0",
    "react": "^19.2.2",
    "react-dom": "^19.2.2",
    "styled-components": "^6.1.15"
  },
  "devDependencies": {
    "@sanity/client": "^6.24.1",
    "@types/react": "^19",
    "typescript": "^5"
  }
}
